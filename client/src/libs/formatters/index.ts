import { formatCep, formatUf } from "./format-address";
import { formatPhone } from "./format-contact";
import { formatCurrency } from "./format-currency";
import { formatDate, formatDateTime } from "./format-date";
import { formatCnpj, formatCpf } from "./format-document";
import { 
  formatNumberOnly, 
  formatAlphabeticalOnly, 
  formatAlphanumericOnly, 
  formatURI 
} from "./format-primitives";

export {
  formatAlphabeticalOnly,
  formatAlphanumericOnly,
  formatDateTime,
  formatCep,
  formatCnpj,
  formatCpf,
  formatCurrency,
  formatDate,
  formatNumberOnly,
  formatPhone,
  formatURI,
  formatUf
}